import React, { Component } from "react";
import { withStyles } from '@material-ui/core/styles';

const useStyles = {
  root: {},
};

class StarShipDetails extends Component {
  render() {
    return <div></div>;
  }
}

export default withStyles(useStyles)(StarShipDetails)
